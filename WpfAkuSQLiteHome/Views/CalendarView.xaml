<UserControl x:Class="WpfAkuSQLiteHome.Views.CalendarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfAkuSQLiteHome.Views"
             xmlns:vm ="clr-namespace:WpfAkuSQLiteHome.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">


    <UserControl.Resources>
        <vm:CalendarViewModel x:Key="CalendarViewModel"/>
    </UserControl.Resources>

    <Grid DataContext="{Binding Source={StaticResource CalendarViewModel}}">
       
        <StackPanel Width="800" Height="200" VerticalAlignment="Bottom">
            <Button x:Name="LoadEvents" HorizontalAlignment="Center" Height="50" Width="250" Content="Load Events"></Button>
            <TextBlock x:Name="One" FontSize="20"></TextBlock>
            <TextBlock x:Name="Two" FontSize="20"></TextBlock>
            <TextBlock x:Name="Three" FontSize="20"></TextBlock>
        </StackPanel>
    </Grid>
</UserControl>
